# 半休/有休の齟齬 — UI手動訂正機能 改修メモ

> **位置づけ**: 実装前の検討・合意を蓄積する「育てるドキュメント」。
> 改修実行日まではコードを変更せず、本MDの充実に集中する。
> **最終更新**: 2026-07-02（コード再分析を反映）
> **文字コード**: UTF-8（BOMなし）
>
> **信頼度の優先順位**: 記述が矛盾する場合、**新しい分析（§2.7・2026-07-02 以降）を旧記述より優先**する。旧記述（§2.1–§2.6 の一部）は当時の理解であり、本MDでは最新分析を正とする。

---

## 0. このMDの使い方（運用ルール）

- 実装日までは **本MDのみを更新** する（コードは触らない）。
- 「確定」「検討中」「要確認」をラベルで明示する。確定事項は後から覆さない。
- コード上の事実（grepで確認済み）と、推測・未確認を **必ず区別** して書く。
- 章末の「要確認リスト」を実装着手前にすべて潰すことをゴールにする。

---

## 1. 課題・ゴール

### 1.1 課題
- 半日有休を取得した場合に、**日報入力データと勤怠（Jobcan）データで齟齬** が出るケースがある。
- 原因はスタッフごとに「休暇タイプ」が異なり「半日」の時間が変動すること。さらに昼休憩の自動計算など複数要素が絡み、**正しい時間を自動算出するロジックが組めない**。

### 1.2 やること（確定）
- **UI側で各スタッフが手動訂正できれば十分** とする。
- 「有休」タスクがある日に限り、有休タスクの **時間 / 時間帯 / `task_total_minutes`** に不整合があれば、**入力値で上書きできる** 機能で完結させる。
- Jobcanから受けた値は **参考値としてUIに提示**。間違いがあればユーザー自身が修正し確定する。

### 1.3 やらないこと（確定）
- **JobcanAPIで適切なデータを得る根本対策は諦める。再検討しない。**
- 現状「中途半端に機能している休暇API取得部分」は **変更も削除もしない**（参考値取得として温存）。
- 半休時間の自動正確計算（スタッフ種別・休暇タイプ・昼休憩を加味した算出）は目指さない。

---

## 2. 現状の仕組み（コードから確認した事実）

> 行番号は 2026-06-22 時点。改修日までにズレる可能性があるため目安。

### 2.1 全体構成（README準拠）
- バックエンド: Flask（`main.py`） / Cloud Run、Firestore。
- 入力UI（スマホ）: `liff-app.js`（工務・ネット両対応、`isReportNetPage` で分岐）。
- 管理画面（PC）: `admin.html` / `admin_net.html` + `js/admin-app.js`、代理入力 `_manager_proxy_report.html` / `_manager_proxy_report_net.html`。
- API: `routes/api.py`。休暇関連ロジックの多くは `app_core/utils.py`。

### 2.2 日報タスクのデータ形状（重要な差分）
`liff-app.js` の送信処理（`1234–1277`）で **工務とネットでタスク形状が異なる**:

| 区分 | エンドポイント | タスクの主なフィールド | 時間の決まり方 |
|------|----------------|------------------------|----------------|
| 工務 | `POST /api/reports` | `categoryA_id/label`, `categoryB_id/label`, **`time`(分)** | `time` を **分で直接入力**（free input） |
| ネット | `POST /api/reports_net` | 上記 + **`startTime`/`endTime`**, `comment` | `startTime`/`endTime`(15分丸め) から **分を導出** |

- 送信ボディ共通: `date`, `taskTotalMinutes`(=全タスク`time`合計 `allocatedMinutes`), `jobcanWorkMinutes`, `tasks`。
- 工務は **有休タスクの`time`を画面でそのまま編集可能**（自由入力）。ネットは **時間帯から導出** されるため、有休タスクの訂正導線が工務と異なる。← **改修ボリュームの主因**

### 2.3 入力画面の既存「差異」挙動（`liff-app.js`）
- `allocatedMinutes`（タスク合計） vs `totalWorkTime`（勤務時間）の差を計算し、`remainingMinutes !== 0` のとき送信時に確認ダイアログ（`1223–1224`「勤務時間と入力した工数に差異があります。このまま送信しますか？」）。
- カテゴリ欄は `readonly`（`292–304`）。**時間欄は編集可**。
- `1552–1569`: 有休情報を取得して `calendarStatuses[date].paid_holiday = 'full'|'half'` にマージ。カレンダー表示用。

### 2.4 有休のJobcan同期（`routes/api.py`）
- `POST /api/sync-paid-holidays`（`5564–`）: 月度の有休等をJobcanから取得し日報へ反映。
- `GET /api/jobcan/paid-holidays`（`5333–`）: 参考値取得。`liff-app.js 3107` の「更新」ボタンから呼ばれる。
- `register_paid_holiday_work_report`（`6010–6158`）が有休タスクを生成/更新:
  - 有休タスク固定値: `categoryA_id="A00"`, `label="有休"`, `categoryB_id="e_000000"`, `label="000000"`, `detail="有休自動適用"`。
  - ネット形式時のみ `startTime`/`endTime`/`comment` を付与（`6055–6058`）。
  - **既存ドキュメントに有休らしきタスクが1件でもあれば、追加も置換もしない**（`6071–6080`、`_task_looks_like_paid_leave` 判定）。
    → **手動訂正後に再同期しても上書きされない**（= やりたいことと整合。重要な既存挙動）。
    → **【2026-07-02 訂正】** 判定は A00/e_000000 だけでなく **`categoryA_id∈{A00, A99}` / `categoryA_label∈{有休, 有休(忌引)}` / `categoryB_id=e_000000`** のいずれか（`api.py:5983`）。上の記述は不完全だった。詳細と含意は §2.7 F2 / R3 / R6 参照。
  - 追加時は `task_total_minutes` を全タスク再合計（`6086–6096`）。

### 2.5 半休の分数決定（`app_core/utils.py`）
- 工務: `resolve_paid_leave_minutes_engineering`（`1193–`）。`holiday_types.minutes` 優先、なければ **full=480 / half=240 にフォールバック**（`1204`）。
- 同期用: `resolve_paid_leave_for_sync`（`1212–`）/ `..._by_work_kind`（`1249–`）が minutes と holiday の開始/終了時刻を返す。
  - **【2026-07-02 注記】** 3つのresolverで複数候補ヒット時の採用ポリシーが不統一（`resolve_paid_leave_for_sync` は「最後」採用 `utils.py:1231-1239`、`_by_work_kind`・`_engineering` は「最初」採用）。参考値の信頼性に関わる。§2.7 R7 参照。
- ネット半休で開始/終了が取れない場合のフォールバック開始時刻推定: `_infer_net_paid_leave_anchor_start`（`api.py:5502`）→ 最終合成 `default_net_paid_leave_time_slot`（`utils.py:1320`、適用は `api.py:5800-5803`）。

### 2.6 齟齬が生まれるメカニズム（整理）
1. 半休の実時間がスタッフ種別/休暇タイプ/昼休憩で変動するのに、同期は `holiday_types.minutes` か **240分の固定フォールバック** で有休タスクを作る。
2. その結果、有休タスク`time`（および`task_total_minutes`）が実態とズレ、`jobcan_work_minutes` との突合（予実）で差異として現れる。
3. ネットは `startTime`/`endTime` 由来のため、時間帯のズレも齟齬要因になる。

---

## 2.7 追加分析（2026-07-02）— 確認済み事実と要再考点

> 2026-07-02 にコードを再確認して追記。**本節は §2.1–§2.6 の旧記述より優先**（矛盾時は本節を正とする）。行番号は 2026-07-02 時点。
> 分類: **確定(事実)** = コードで確認済み / **未定** = 方針未決定・実装時に判断する。

### 確定(事実)

- **F1. 保存は全上書き**: `/api/reports`（`api.py:880`）も `/api/reports_net`（`api.py:1005`）も `doc_ref.set(...)` でドキュメントを**全フィールド上書き**し、`task_total_minutes` は受信値でなく**サーバが tasks から再計算**（工務 `api.py:916,990` / ネット `:1067` 付近）。→ 案A（time/時間帯を編集して再送信）で正しい合計が保存される前提は成立。
- **F2. 再同期の非破壊**: sync は「有休らしきタスクが1件でもあれば追加も置換もしない」（`api.py:6071-6080`）。判定 `_task_looks_like_paid_leave`（`api.py:5983`）は **`categoryA_id∈{A00, A99}` / `categoryA_label∈{有休, 有休(忌引)}` / `categoryB_id=e_000000`** のいずれかで真。
- **F3. ネット自動有休は必ず開始/終了を持つ**: sync のネット経路は最終的に `default_net_paid_leave_time_slot`（`utils.py:1320`）で開始/終了を**合成**する（`api.py:5800-5803`）。→ sync 由来のネット有休タスクは start/end 欠落しない。
- **F4. ネット送信は不完全行を破棄**: ネット送信は `if (!majorVal || !minorVal || !startTime || !endTime) return;`（`liff-app.js:1356`）で、start/end 欠落の行を**黙って落とす**（バリデーションエラーにはならない）。

### 要再考点（未定・実装時に判断する）

- **R1. 参考値の出所ミスマッチ（優先度: 高）**: 参考値取得 `GET /api/jobcan/paid-holidays`（`api.py:5335`、フロント呼び出し `liff-app.js:3259`）は Jobcan の**生 `use_days` をそのまま返す**だけで、sync が有休タスクへ書く**解決済みの値**（`resolve_paid_leave_for_sync` ＋ 240分フォールバック ＋ 合成時間帯）とは**別パイプライン**。案A/Bで参考値にこの生データを使うと、自動入力済みタスクの値と食い違う恐れ。→ **未定**: 参考値を sync と同じ解決結果に揃えるか（新エンドポイント or resolver 結果の露出）。§7 該当項目を「不十分の可能性」に更新済み。
- **R2. 齟齬判定の偽陽性（優先度: 高）**: full有休の新規作成は `jobcan_work_minutes:0`（`api.py:6145`）なのに `task_total_minutes=480`。判定を「`task_total_minutes ≠ jobcan_work_minutes`」にすると**全 full 有休日が恒常的に齟齬**扱いになる。→ **未定**: 齟齬判定基準（有休タスク`time` vs 参考値 / 時間帯ズレ 等）。§7 で最優先に確定させる。
- **R3. 検知ロジックの二重管理（優先度: 中）**: 「有休あり日」検知を JS(`liff-app.js`)側に作ると、正である Python `_task_looks_like_paid_leave`（F2）とドリフトし得る（UIで訂正対象なのに sync が非スキップ／その逆）。→ **未定**: 判定条件を単一仕様として固定し、JS実装を厳密一致させ、テストで担保するか。
- **R4. ネット不完全行のサイレント消滅（安全性）**: F4 により、レガシー／手動作成／ユーザーが時刻をクリアしたネット有休行は、訂正作業中に**タスクごと消える**危険。→ **未定**: 訂正UIで「不完全な有休行」を検知して警告／送信ブロックするか（§6.3 完了条件に反映済み）。
- **R5. `set()` 全上書きと案Cフラグ（安全性）**: F1 の全上書きのため、案Cで `manual_override` 等を足すと**送信のたびにクライアントが再送しないとフラグが消える**。→ **未定**: 案C採用時にフラグをクライアントでラウンドトリップさせるか、保存を merge/update に変えるか。§3 案C に注記済み。
- **R6. Jobcan側変更の非伝播＋削除経路なし（スコープ境界）**: F2 により、Jobcanで有休が half→full 変更／取消されても再同期で反映されず、有休タスクの**削除経路も無い**（sync は追加のみ）。本機能（time／時間帯の訂正）では「そもそも有休でない日／種別変更」は直せない。→ **未定**: スコープ外として運用注記に留めるか、案Cの「auto(更新可)／確定(固定)」区別で将来対応するか。
- **R7. resolver の候補選択が不統一（参考値の信頼性）**: `resolve_paid_leave_for_sync` は複数候補で**「最後」採用**（`utils.py:1231-1239`）、`_by_work_kind`・`_engineering` は**「最初」採用**（break あり）。複数 `holiday_types` 候補が該当すると参考値 minutes が経路依存で非決定的。「utils.py は触らない」方針だが**把握必要**。→ **未定**: 参考値提示時にこの非決定性を許容するか。

---

## 3. 解決アプローチ（複数案を列挙）

> 共通方針: 「有休タスクのある日」を検出 → 参考値（Jobcan）と入力値を提示 → ユーザーが上書き確定。
> 再同期で上書きされない既存挙動（2.4）を前提にできるのが追い風。

### 案A: 既存の時間入力欄を活かす最小改修（推奨の出発点）
- **工務**: 有休タスクの`time`欄は既に編集可能。**「有休あり日」を検知して参考値バッジ/警告を表示** + 確定時に `task_total_minutes` を正しく再計算するだけ。
- **ネット**: 有休タスクの `startTime`/`endTime` を編集→分を導出する既存導線を有休タスクにも適用し、参考値を併記。
- メリット: 改修面が小さい。新規UIコンポーネントほぼ不要。既存フォームの延長。
- デメリット: 「不整合の検知・可視化」をどこまで作るかで工数が動く。ネットの有休タスク編集導線が現状どこまで効くか要確認。
- 向くケース: まず工務スマホでMVPを通したい場合。

### 案B: 「齟齬バナー＋ワンクリック上書き」UI（体験重視）
- 有休あり日で「参考値 N分 / 入力値 M分 が不一致」をバナー表示し、「参考値で上書き」「入力値を維持」を選べる。
- メリット: ユーザーが齟齬を見落としにくい。訂正操作が明快。
- デメリット: 検知ロジック＋UIコンポーネントを4面ぶん作る必要があり工数増。
- 向くケース: 運用者がミスを減らしたい/問い合わせを減らしたい場合。

### 案C: バックエンドに「確定フラグ/手動上書きフラグ」を持たせる
- 有休タスクに `manual_override: true`（や `source: "manual"`）を付与し、同期側はこれを見て確実にスキップ。`task_total_minutes` の正本もこのフラグで判定。
- メリット: 「ユーザー確定済み」を明示でき、将来の同期仕様変更にも頑健。突合・締め処理での判定にも使える。
- デメリット: Firestoreスキーマ追加、既存データのマイグレーション/後方互換の検討が必要。
- 注意: 2.4の現状でも「有休タスクが既にあればスキップ」なので、**最小要件には必須ではない**。堅牢性のための上位互換オプション。
- **【2026-07-02 追記】注意（安全性 R5）**: 保存は `doc_ref.set()` の全上書き（§2.7 F1）。案Cのフラグは**通常送信のたびにクライアントが再送しないと消える**。採用時は (a) クライアントで全フラグをラウンドトリップさせる、または (b) 保存を merge/update に変える、のいずれかを設計に含めること。
- **【2026-07-02 追記】動機（R6）**: 「auto（更新可）／確定（固定）」を区別できれば、Jobcan側で有休が変更・取消された場合の再同期・整理も将来的に扱える（現状は追加のみで削除経路なし）。

### 案D: 突合（予実）側だけで齟齬を吸収（入力フォームを触らない）
- 入力フォームは変えず、`admin.html` の予実突合表示側で「有休あり日は差異を許容/注記」する。
- メリット: 入力UI（スマホ2面）を触らずに済む。
- デメリット: 「各スタッフが手動訂正する」という要件（1.2）から外れる。ユーザーが直せない。
- 位置づけ: 単独では要件不足。案A/Bの補助として検討。

> **たたき台の推奨**: 「案A をベースに、案B の最小の可視化（バッジ/注記）を載せる。案C はフェーズ2で堅牢化として検討」。最終決定は要確認リスト解消後。

---

## 4. 影響範囲マップ（4面 × レイヤ）

対象スコープは4面（確定）:
1. 工務 LIFFアプリ（スマホUI）
2. 工務 PC管理画面
3. ネット事業部 LIFFアプリ（スマホUI）
4. ネット事業部 PC管理画面

| レイヤ / ファイル | 工務スマホ | 工務PC | ネットスマホ | ネットPC | 備考 |
|---|---|---|---|---|---|
| `liff-app.js` | ◎ | – | ◎ | – | 入力フォーム/有休検知/参考値表示/送信 |
| `js/admin-app.js` | – | ◎ | – | ◎ | 代理入力・予実突合 |
| `_manager_proxy_report.html` | – | ○ | – | – | 工務 代理入力フォーム |
| `_manager_proxy_report_net.html` | – | – | – | ○ | ネット 代理入力フォーム |
| `admin.html` / `admin_net.html` | – | ○ | – | ○ | 突合/カレンダー表示 |
| `routes/api.py` | △ | △ | △ | △ | 上書き保存は既存 `/api/reports`・`/api/reports_net` で足りる想定。案C採用時のみ要改修 |
| `app_core/utils.py` | – | – | – | – | **触らない**（参考値ロジック温存=やらないこと） |
| Firestore `daily_reports` | △ | △ | △ | △ | 案C採用時のみフィールド追加 |

凡例: ◎主改修 / ○改修あり / △条件付き / –原則対象外

**ネット特有の注意**: 時間が `startTime`/`endTime` 由来のため、有休タスクの訂正は「時間帯の訂正」になる。工務（分の直接入力）とUIモデルが異なるため、案の実装も面ごとに分ける必要がある。

---

## 5. 改修ボリューム見積もり（粗）

> 前提: 案A＋最小可視化。確定前のラフ見積り。1人日=実働ベース。要確認解消で増減する。

| 面 | 主な作業 | 規模感 | 不確実性 |
|---|---|---|---|
| 工務スマホ | 有休あり日検知、参考値バッジ、`task_total_minutes`再計算確認、送信確認 | **S（0.5〜1人日）** | 低（time欄が既に編集可） |
| 工務PC | 代理入力フォームへ同等反映、突合表示の注記 | **S〜M（1〜1.5人日）** | 中 |
| ネットスマホ | 有休タスクの時間帯訂正導線、参考値併記、導出分の整合 | **M（1.5〜2.5人日）** | 中〜高（start/end由来） |
| ネットPC | 代理入力フォームへ同等反映、突合表示 | **M（1.5〜2.5人日）** | 中〜高 |
| 横断 | 齟齬検知の共通仕様、文言、QA、回帰確認 | **M（1〜2人日）** | 中 |
| （案C採用時の追加） | スキーマ追加・マイグレ・同期側判定・後方互換 | **+M〜L（2〜4人日）** | 高 |

- **MVP（案A・工務スマホのみ）合計の目安: 約1〜2人日**。
- **4面フル（案A＋可視化）合計の目安: 約6〜10人日**。
- 案C（堅牢化）を足すと +2〜4人日。

> ※あくまで概算。実装日に要確認リストを潰してから精緻化する。

---

## 6. スコープ分割・MVP・進行フロー

### 6.1 MVPの定義（たたき台）
- **MVP = 工務スマホで「有休あり日に参考値を見ながら時間を訂正し、正しい `task_total_minutes` で確定できる」**。
- 既存の再同期スキップ挙動（2.4）により、訂正値が同期で壊れないことを確認する。

### 6.2 進行順（たたき台・確定は要相談）
1. **工務スマホ**（最小・既存time欄活用でリスク低）→ MVP検証
2. **工務PC**（代理入力に横展開、突合の注記）
3. **ネットスマホ**（時間帯モデルの訂正導線）
4. **ネットPC**（代理入力へ横展開）

> 工務を先に通してパターンを確立 → ネットへ展開、の順。各面は「検知 → 参考値提示 → 上書き確定 → 同期非破壊の確認」を1セットとする。

### 6.3 各面の完了条件テンプレ（案）
- [ ] 有休タスクのある日を検知して表示できる
- [ ] Jobcan参考値（分/時間帯）を併記できる
- [ ] ユーザーが入力値で上書き・確定できる
- [ ] `task_total_minutes` が訂正後の値で正しく保存される
- [ ] 再同期しても訂正値が壊れない
- [ ] 予実突合の表示が訂正後に矛盾しない
- [ ] **（ネット）不完全な有休行（start/end 欠落）を検知し、サイレント消滅（§2.7 F4/R4）を防げる**
- [ ] **参考値が sync の解決値と一致している（生 use_days との食い違いがない。§2.7 R1）**

---

## 7. 要確認リスト（実装着手前に潰す）

> 凡例: **[確認済み]** = 2026-07-02 分析でコード事実が判明（決定不要）。**[未定]** = 方針が未決定で、実装着手前に判断が必要。

- [x] **[確認済み]** 工務スマホで有休タスクの `time` を編集して再送信したとき `task_total_minutes` は期待通りになる → `/api/reports` はサーバ側で tasks 合計を再計算（§2.7 F1）。
- [x] **[確認済み]** 半休で `startTime`/`endTime` が取れないケースでも、sync のネット経路は合成フォールバックで**必ず付与**（§2.7 F3、`api.py:5800-5803`）。ただし表示・訂正の方針は **[未定]**（下記参照）。
- [ ] **[一部確認/未定] ネットスマホ/PCで有休タスクの `startTime`/`endTime` を編集する導線**: 自動生成分は start/end を持ち編集で分数を再計算できる見込み（§2.7 F3/F4）。ただし**不完全行はサイレント消滅**（§2.7 R4）するため、その検知・防止策は **[未定]**。
- [ ] **[未定] 「齟齬あり」の判定基準**（§2.7 R2）。`task_total_minutes ≠ jobcan_work_minutes` は full有休で偽陽性になるため不可。有休タスク`time` ≠ 参考値／時間帯ズレ 等で定義する。**最優先で確定させる**。
- [ ] **[未定] 参考値の出所**（§2.7 R1）。既存 `GET /api/jobcan/paid-holidays` は**生 `use_days` を返すだけ**で sync の解決値と別物。参考値を解決値に揃えるか（新エンドポイント等）を決める。
- [ ] **[未定] 検知ロジックの単一仕様化**（§2.7 R3）。JS 検知を Python `_task_looks_like_paid_leave` と厳密一致させ、テストで担保するか。
- [ ] **[未定/要調査] 代理入力（PC）で有休あり日**を、スマホと同じ検知/表示で流用できるか（`js/admin-app.js` の構造は未調査）。
- [ ] **[未定] 案Cを採るか**（`manual_override` 等のフラグ）。採る場合の**既存データ後方互換**・締め処理/突合への波及・`set()` 全上書き対策（§2.7 R5）。将来の Jobcan 側変更への追従（§2.7 R6）の動機にもなる。
- [ ] **[未定] 文言**（バッジ／警告／確認ダイアログ）の確定。
- [x] **[確認済み]** 触らない範囲の境界: `app_core/utils.py` の参考値ロジックは温存（§1.3・§4 の確定事項）。ただし複数候補時の採用ポリシー非統一（§2.7 R7）は把握しておく。

---

## 8. 関連コード索引（grep結果・2026-07-02 更新）

> **【2026-07-02 更新】** `liff-app.js` は n_logistics コメント機能の追加で先頭付近に約110行増えたため、旧行番号（2026-06-22 記載）から**全体的に約 +113 ズレ**ている。以下は 2026-07-02 時点の現行値。関数名・シンボルで grep するのが確実。

- 日報保存(工務・全上書き): `routes/api.py:880` `post_report`
- 日報保存(ネット・全上書き): `routes/api.py:1005` `post_net_report`
- 有休タスク生成: `routes/api.py:6010` `register_paid_holiday_work_report`
- 有休らしさ判定: `routes/api.py:5983` `_task_looks_like_paid_leave`
- 有休同期: `routes/api.py:5566` `sync_paid_holidays`
- ネット有休の時刻解決＋合成フォールバック適用: `routes/api.py:5800-5803`
- 参考値取得API(生 use_days): `routes/api.py:5335` `get_jobcan_paid_holidays`
- ネット半休フォールバック開始時刻推定: `routes/api.py:5502` `_infer_net_paid_leave_anchor_start`
- 半休分数(工務): `app_core/utils.py:1193` `resolve_paid_leave_minutes_engineering`
- 同期用解決: `app_core/utils.py:1212` `resolve_paid_leave_for_sync` / `1249` `..._by_work_kind`
- ネット時刻の合成フォールバック: `app_core/utils.py:1320` `default_net_paid_leave_time_slot`
- スマホ送信(工務/ネット分岐・ネット不完全行スキップは `1356`): `liff-app.js:1344` 付近、エンドポイント選択 `1394`
- 有休カレンダーマージ: `liff-app.js:1678`
- 「更新」ボタン同期呼び出し(`/api/sync-paid-holidays`): `liff-app.js:3216`、参考値取得(`/api/jobcan/paid-holidays`): `liff-app.js:3259`

---

## 9. 変更履歴

| 日付 | 内容 |
|------|------|
| 2026-06-22 | 初版作成。課題整理・現状調査・アプローチ列挙・影響範囲・ボリューム見積り・MVPフロー・要確認リスト。 |
| 2026-07-02 | コード再分析を反映。§2.7 追加（確認済み事実 F1–F4／要再考点 R1–R7）。§2.4 の有休判定を A99・忌引まで訂正。§2.5 に resolver 採用ポリシー非統一を注記。§3 案C に `set()` 全上書き対策（R5）を追記。§6.3 完了条件・§7 要確認を更新（確認済み/未定を明示）。§8 索引を現行行番号に更新（liff-app.js は約+113ズレ）。 |
